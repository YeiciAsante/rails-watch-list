<div class="container">
   <div class="row">
     <% @lists.each do |list| %>
       <div class="col-12">
         <%= link_to list.name, list_path(list) %>
       </div>
     <% end %>
     <%= link_to "Create a new list", new_list_path %>
   </div>
 </div>
